{
    "name": "your-app",
    "version": "1.0.0",
    "scripts": {
      "build": "cd server && npm install && npm run build"
    },
    "dependencies": {},
    "devDependencies": {},
    "engines": {
      "node": "21.1.0"
    }
  }